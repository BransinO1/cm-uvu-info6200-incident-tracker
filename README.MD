# Run : "python web_app.py" to start the web server
# Access the application at http://127.0.0.1:5000
# Or run "python -m waitress --listen=127.0.0.1:5000 web_app:app" to use the Waitress production server.
# Login with the credentials you created during registration to view and manage your incidents. (e.g. 1, 1)
# View the v1 list at http://127.0.0.1:5000/api/v1/incidents
# View the v1 list single item at http://127.0.0.1:5000/api/v1/incidents/4

# Note to self: Ensure you have already installed Flask and Flask_SQLAlchemy in your Python environment.
# You can install them using the pip command below:
# pip install Flask Flask_SQLAlchemy werkzeug waitress